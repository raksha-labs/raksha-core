# Historical Replay Scenarios

These scenario definitions are consumed by `apps/history-worker` to populate:

- `history.cases`
- `history.case_events`
- `history.replay_catalog`

for the Replay Lab history catalog.

Primary source files are maintained in `raksha-simlab/scenarios/historical`. Keep both locations aligned when updating scenarios.
