aws_region              = "eu-west-1"
environment             = "prod"
compute_mode            = "fargate_mix"
budget_limit_usd        = 1000
# Optional cost controls:
# anomaly_total_impact_absolute_usd   = 20
# enable_billing_estimated_charges_alarm = true
# billing_estimated_charges_alarm_usd = null  # null => uses budget_limit_usd
alarm_emails            = ["you@example.com"]
admin_access_mode       = "private-only"
enable_public_https     = true
public_certificate_arn  = "arn:aws:acm:eu-west-1:123456789012:certificate/replace-me"
enable_waf              = true
image_tag               = "v1.0.0"

github_org              = "raksha-labs"
github_repo             = "raksha-core"
github_allowed_branches = ["master", "release/*"]

create_oidc_provider    = false
# oidc_provider_arn     = "arn:aws:iam::<account-id>:oidc-provider/token.actions.githubusercontent.com"

# RPC WebSocket URLs for the indexer — stored as Secrets Manager secrets.
# Without this the indexer runs in mock/synthetic-data mode.
#
# 1. Create the secret:
#    aws secretsmanager create-secret \
#      --name raksha/prod/rpc \
#      --secret-string '{"ETH_WS_URL":"wss://...","BASE_WS_URL":"wss://..."}'
#
# 2. Set the ARNs here:
# rpc_ws_url_secret_arns = {
#   ETH_WS_URL  = "arn:aws:secretsmanager:eu-west-1:123456789012:secret:raksha/prod/rpc-AbCdEf:ETH_WS_URL::"
#   BASE_WS_URL = "arn:aws:secretsmanager:eu-west-1:123456789012:secret:raksha/prod/rpc-AbCdEf:BASE_WS_URL::"
# }
